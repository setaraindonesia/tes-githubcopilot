{
  "name": "setaradapps",
  "version": "1.0.0",
  "description": "Aplikasi Super Terdesentralisasi - Chat, Marketplace, DeFi, IoT",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:restart": "docker-compose restart",
    "db:migrate": "cd services/auth-service && npm run migrate",
    "db:seed": "cd services/auth-service && npm run seed",
    "db:reset": "docker-compose down -v && docker-compose up -d postgres && sleep 10 && npm run db:migrate && npm run db:seed",
    "setup": "bash setup.sh",
    "setup:win": "powershell -ExecutionPolicy Bypass -File setup.ps1",
    "services:dev": "turbo run dev --filter=./services/*",
    "apps:dev": "turbo run dev --filter=./apps/*",
    "packages:build": "turbo run build --filter=./packages/*",
    "chat:test:setup": "node scripts/create-test-users.js",
    "chat:test:start": "powershell -ExecutionPolicy Bypass -File scripts/start-chat-testing.ps1",
    "start:dev": "npm run auth:dev",
    "auth:dev": "cd services/auth-service && npm install && npm run build && npm run start:dev",
    "auth:build": "cd services/auth-service && npm run build",
    "auth:start": "cd services/auth-service && npm run start:prod",
    "auth:test": "cd services/auth-service && npm run test",
    "auth:seed": "cd services/auth-service && npm run seed",
    "chat:dev": "cd services/chat-service && npm install && npm run build && npm run start:dev",
    "chat:build": "cd services/chat-service && npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  },
  "overrides": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "decentralized",
    "chat",
    "marketplace",
    "defi",
    "iot",
    "blockchain",
    "ai"
  ],
  "author": "Setaradapps Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.5.0"
  }
}
